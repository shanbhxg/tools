{
  "name": "tools",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && electron-packager . --platform=win32 --arch=x64 --out=dist --overwrite --ignore-certificate"
  },
  "config": {
    "strict-ssl": false
  },
  "author": "Deepti",
  "license": "ISC",
  "dependencies": {
    "electron-packager": "^15.2.0",
    "electron-squirrel-startup": "^1.0.0"
  },
  "devDependencies": {
    "electron": "^6.1.2"
  }
}
